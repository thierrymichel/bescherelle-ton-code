var something=true
let   nb = .5
let   output = {}
var   data = { 'foo': 'bar', baz: "qux" }
var   x = (() => {
          console.info();
          return
}).bind(bar);
    if(false===something)
        {
  // uppercase comment
console.info(data);;;;
        }else
        {
console.info('no data');
    }
if   (!!something) output.result = (nb * nb)
if(output.result<nb)
console.info('Result:' + output["result"],x)
